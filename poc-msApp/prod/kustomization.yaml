apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# namespace: ms

resources:
- ../base/
# - escrita/
# - leitura/

configMapGenerator:
  - name: escrita-config
    files:
    - escrita/configmap.yaml
  - name: leitura-config
    files:
    - leitura/configmap.yaml

secretGenerator:
  - name: escrita-secret
    files:
    - escrita/secret.yaml
  - name: leitura-secret
    files:
    - leitura/secret.yaml
